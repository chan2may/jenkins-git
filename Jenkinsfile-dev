def DevPipeline() {
    echo "Running pipeline Function"

        stage('env.BRANCH_NAME') {
            steps {
                sh '''
                    echo 'Pulling Branch ...' + env.BRANCH_NAME
                '''
            }
        }
}

